use asdf
[[ ! ( "$PYTHONPATH" =~ "pdm" ) ]] && eval "$(pdm --pep)"
dotenv
env_vars_required REPOSITORY_USER REPOSITORY_PASSWORD

for binpath in "$(find . __pypackages__ -maxdepth 2 -type d -name bin -print0)"; do
    PATH_add $binpath
done
